<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- NCF Management Groups -->
    <record id="group_ncf_user" model="res.groups">
        <field name="name">NCF User</field>
        <field name="category_id" ref="base.module_category_accounting"/>
        <field name="implied_ids" eval="[(4, ref('account.group_account_user'))]"/>
        <field name="comment">Users can view NCF information and assignments</field>
    </record>

    <record id="group_ncf_manager" model="res.groups">
        <field name="name">NCF Manager</field>
        <field name="category_id" ref="base.module_category_accounting"/>
        <field name="implied_ids" eval="[(4, ref('group_ncf_user')), (4, ref('account.group_account_manager'))]"/>
        <field name="comment">Users can manage NCF sequences and configuration</field>
    </record>

    <!-- Record Rules -->
    
    <!-- NCF Sequence Multi-Company Rule -->
    <record id="ncf_sequence_company_rule" model="ir.rule">
        <field name="name">NCF Sequence: multi-company</field>
        <field name="model_id" ref="model_ncf_sequence"/>
        <field name="domain_force">[('company_id', '=', user.company_id.id)]</field>
        <field name="global" eval="True"/>
    </record>

    <!-- NCF Assignment Multi-Company Rule -->
    <record id="ncf_assignment_company_rule" model="ir.rule">
        <field name="name">NCF Assignment: multi-company</field>
        <field name="model_id" ref="model_ncf_assignment"/>
        <field name="domain_force">[('company_id', '=', user.company_id.id)]</field>
        <field name="global" eval="True"/>
    </record>

    <!-- NCF Sequence Management Restrictions -->
    <record id="ncf_sequence_manager_rule" model="ir.rule">
        <field name="name">NCF Sequence: managers only for critical operations</field>
        <field name="model_id" ref="model_ncf_sequence"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_ncf_manager'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

</odoo>
